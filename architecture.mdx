---
title: Architecture
description: How the SDK, Gennoctua backend, and HP AI fit together.
---

## System Overview

```mermaid
flowchart TD
    subgraph Browser["Brand's Website (Browser)"]
        SDK["@gennoctua/personalize-core\n─────────────────────────\n• Face detection (face-api.js)\n• Pose scoring (MediaPipe)\n• Room detection (YOLOv8 ONNX)\n• Photo selection (person + room)\n• Job submission & polling\n• Result caching (IndexedDB)\n• Rate limiting"]
    end

    subgraph Proxy["Auth Proxy (one of)"]
        A["A) Gennoctua Backend\ntoken.gennoctua.com\n← frontend-only brands"]
        B["B) Brand's Own Backend\nbrand.com/api/tryon\n← full-stack brands"]
    end

    HP["HP AI\n(AI try-on engine)"]

    Browser -->|HTTPS| Proxy
    Proxy -->|HTTPS + API Key| HP
```

---

## Two Auth Paths

### Path A — Frontend-Only (via Gennoctua)

Used by brands with no backend (Shopify, Webflow, plain HTML).

```mermaid
sequenceDiagram
    participant SDK as Browser SDK
    participant GN as Gennoctua Backend
    participant HP as HP AI

    SDK->>GN: POST /auth/token { publicKey: "gn_pk_..." }
    GN-->>SDK: { token: "eyJ...", expiresIn: 3600 }

    SDK->>GN: POST /api/tryon/submit (Bearer token)
    GN->>HP: forwards request (X-API-Key: hpk_...)
    HP-->>GN: { job_id: "..." }
    GN-->>SDK: { job_id: "..." }

    SDK->>GN: GET /api/tryon/status/:jobId (Bearer token)
    GN->>HP: SSE stream
    HP-->>GN: status updates
    GN-->>SDK: streams SSE back to browser
```

### Path B — Full-Stack (brand's own backend)

Used by brands that have their own Node.js, Next.js, Express, etc. backend.

```mermaid
sequenceDiagram
    participant SDK as Browser SDK
    participant Brand as Brand's Backend
    participant HP as HP AI

    SDK->>Brand: POST /auth/tryon-token
    Brand-->>SDK: { token: "eyJ...", expiresIn: 3600 }

    SDK->>Brand: POST /api/tryon/submit (Bearer token)
    Brand->>HP: forwards request (HP_API_KEY from .env)
    HP-->>Brand: { job_id: "..." }
    Brand-->>SDK: { job_id: "..." }

    SDK->>Brand: GET /api/tryon/status/:jobId (Bearer token)
    Brand->>HP: SSE stream
    HP-->>Brand: status updates
    Brand-->>SDK: streams SSE back to browser
```

---

## What Runs Where

| Component | Where it runs | What it does |
|---|---|---|
| Face detection | **Browser** (WebAssembly) | Detects faces in uploaded photos |
| Pose scoring | **Browser** (WebAssembly) | Scores body poses for best photo selection |
| Room detection | **Browser** (ONNX / WebAssembly) | Detects furniture objects to classify room type |
| Photo selection | **Browser** + **proxy → Hyperpersona** (LLM) | Picks best person photo per gender, best room photo per room type |
| Token generation | **Backend** (Gennoctua or brand) | Issues short-lived JWTs |
| Job submission | **Backend** (proxy) | Forwards multipart form to HP |
| SSE streaming | **Backend** (proxy) | Streams job status back to browser |
| Result caching | **Browser** (IndexedDB) | Caches try-on results for 7 days |
| AI generation | **HP** | Generates the try-on image |

---

## Data Flow: Full Try-On Lifecycle

```mermaid
flowchart TD
    A["sdk.init()"] --> A1["Load face-api.js + MediaPipe\n(CDN, ~5-10s first time)"]
    A --> A2["Fetch JWT token\nfrom auth endpoint"]

    B["sdk.ingestImages(files)"] --> B0["Filter valid image files\nmax 80"]
    B0 --> BP["Person path\n(fashion, eyewear, footwear...)"]
    B0 --> BR["Room path\n(furniture)"]

    BP --> BP1["Face detection\ngender + age per face"]
    BP1 --> BP2["Pose scoring\nper full-body photo"]
    BP2 --> BP3["LLM picks best\nper gender\nPOST /api/profile/select"]

    BR --> BR1["YOLOv8 object detection\nper photo"]
    BR1 --> BR2["Bucket by room type\nbedroom / living room / dining room"]
    BR2 --> BR3["LLM picks best\nper room type\nPOST /api/room/select"]

    BP3 --> DONE["SelectionSummary returned"]
    BR3 --> DONE

    C["sdk.personalize(opts)"] --> C1{"Cache hit?\nIndexedDB"}
    C1 -->|Yes| C2["Return instantly\ncacheHit: true"]
    C1 -->|No| C3{"Rate limited?"}
    C3 -->|Yes| C4["throw RATE_LIMITED_CLIENT"]
    C3 -->|No| C5["POST /submit → job_id"]
    C5 --> C6["SSE stream /status/:jobId\n(person + furniture), max ~3 min"]
    C6 --> C7["Cache result IndexedDB\n7 day TTL"]
    C7 --> C8["Return PersonalizationResult\n{ imageUrl, cacheHit, jobId }"]
```

---

## HP API

HP is the AI backend that generates the try-on images. Gennoctua manages the relationship with HP — brands never interact with it directly.

| Endpoint | Method | Purpose |
|---|---|---|
| `/api/tryon/submit` | POST | Submit a try-on job |
| `/api/tryon/status/:jobId` | GET (SSE) | Stream job status |
| `/api/eyewear/submit` | POST | Eyewear category |
| `/api/eyewear/status/:jobId` | GET (SSE) | Eyewear status |
| `/api/footwear/submit` | POST | Footwear category |
| `/api/makeup/submit` | POST | Makeup category |
| `/api/accessories/submit` | POST | Accessories category |

The SDK automatically routes to the correct endpoint based on `productType`.

---

## Security Model

| Threat | Mitigation |
|---|---|
| HP API key exposed in browser | Key lives on backend only, never sent to browser |
| Stolen JWT used to make requests | JWTs expire in 1hr, rate limiting caps abuse |
| Brand key leaked | Only gets JWTs, cannot directly call HP |
| Excessive requests (abuse) | Client-side + server-side rate limiting |
| Expired token | SDK auto-refreshes in PublicKey mode, `getToken` called again in proxy mode |
